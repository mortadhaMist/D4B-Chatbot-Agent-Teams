const fs=require('fs');const vm=require('vm');const path=require('path');const p=path.join(__dirname,'..','server.js');const s=fs.readFileSync(p,'utf8');const lines=s.split(/\r?\n/);for(let i=50;i<=lines.length;i+=10){const chunk=lines.slice(0,i).join('\n');try{new vm.Script(chunk,{filename:p});}catch(e){console.log('error at prefix lines',i,e.message);process.exit(0);} }console.log('no error in prefixes');
